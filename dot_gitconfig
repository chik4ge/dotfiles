[user]
    name = chikage
    email = 35529908+chik4ge@users.noreply.github.com

[push]
    default = current

[pull]
    rebase = true

[fetch]
    prune = true

[status]
    showUntrackedFiles = all

[core]
    autocrlf = input
    ignorecase = false
    quotepath = false

[color]
    ui = true
    diff = auto
    status = auto
    branch = auto

[filter "lfs"]
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true
